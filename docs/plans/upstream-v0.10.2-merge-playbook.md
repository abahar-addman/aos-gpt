# v0.10.2 Merge Resolution Playbook

> Per-file resolution for the 129 non-trivial conflicts, from the read-only classification pass (2026-07-13).
> Refs: BASE `v0.8.3`, FORK `HEAD` (v10-update), TARGET `v0.10.2`. Companion to `upstream-v0.10.2-upgrade-plan.md`.
> **i18n** (60 pruned-locale conflicts) resolve as a batch `git rm` (English-only) and are NOT listed here.

## Tallies

| Resolution | Count |
|---|---|
| `take-upstream` | 41 |
| `keep-ours` | 15 |
| `hand-merge` | 56 |
| `keep-deleted` | 12 |
| `accept-upstream-deletion` | 5 |
| **total** | **129** |

Risk: **25 high**, 30 med, 74 low.

## `take-upstream` (41)

| Risk | File | Subsystem | Fork→Upstream | Note |
|---|---|---|---|---|
| low | `backend/open_webui/models/chats.py` | datadog | cosmetic→semantic | Fork only lowered 3 log.info->log.debug lines; upstream did a ~1200-line async-DB rewrite of ChatTable, so take upstream wholesale and reapplying the debug-level tweak is optional/low-value. |
| low | `backend/open_webui/models/knowledge.py` | datadog | cosmetic→semantic | Fork replaced 3 bare print(e) with log.exception; take upstream's ~700-line rewrite, then verify it uses proper logging (not print)-if upstream still prints, reapply log.exception for Datadog structured logs. |
| low | `backend/open_webui/models/users.py` | datadog | cosmetic→semantic | Fork added a module logger (import logging + log=getLogger) and one print(e)->log.exception; take upstream's ~460-line rewrite which has its own logging, reapply optional. |
| low | `backend/open_webui/retrieval/loaders/datalab_marker.py` | datadog | cosmetic→semantic | OCR/document loader; fork only lowered 5 log.info->log.debug with no logic change, so take upstream's loader rewrite safely. |
| low | `backend/open_webui/retrieval/loaders/mineru.py` | datadog | cosmetic→semantic | OCR loader; fork only lowered ~15 log.info->log.debug with no logic change, so take upstream's rewrite safely. |
| low | `backend/open_webui/retrieval/loaders/mistral.py` | datadog | cosmetic→semantic | OCR loader; fork only lowered ~16 log.info->log.debug with no logic change, so take upstream's rewrite safely. |
| low | `backend/open_webui/retrieval/models/colbert.py` | datadog | cosmetic→cosmetic | Pure formatting/log-level clash: fork lowered 1 log.info->log.debug on the same line upstream reformatted (quotes/import reorder)-take upstream, negligible either way. |
| low | `backend/open_webui/retrieval/models/external.py` | datadog | cosmetic→cosmetic | Pure formatting/log-level clash: fork lowered 2 log.info->log.debug in the reranker predict region upstream reformatted (quotes/signature)-take upstream. |
| low | `backend/open_webui/retrieval/utils.py` | datadog | cosmetic→semantic | Core RAG retrieval utils; fork only lowered 4 log.info->log.debug while upstream did a ~1000-line rewrite-take upstream, reapplying the debug tweak is optional. |
| low | `backend/open_webui/retrieval/vector/dbs/milvus.py` | datadog | cosmetic→semantic | Milvus vector-DB client; fork only lowered ~12 log.info->log.debug with no logic change while upstream rewrote it, so take upstream safely. |
| low | `backend/open_webui/retrieval/vector/dbs/opengauss.py` | datadog | semantic→semantic | Fork only downgraded 3 log.info->log.debug for Datadog noise; upstream fully rewrote the client (92/129 lines) so take upstream and optionally re-apply the info->debug downgrades. |
| low | `backend/open_webui/retrieval/vector/dbs/oracle23ai.py` | datadog | semantic→semantic | Fork only did ~22 log.info->log.debug/warning downgrades (Datadog); upstream rewrote the client (124/166), so take upstream and optionally re-apply the level downgrades. |
| low | `backend/open_webui/retrieval/vector/dbs/pinecone.py` | datadog | semantic→semantic | Fork only downgraded 7 log.info->log.debug (Datadog); upstream rewrote the client (107/171), so take upstream and optionally re-apply the downgrades. |
| low | `backend/open_webui/retrieval/web/brave.py` | datadog | semantic→semantic | Fork only bumped one rate-limit log.info->log.warning; upstream rewrote the search fn (38/33), so take upstream and optionally re-apply the level bump. |
| low | `backend/open_webui/retrieval/web/exa.py` | datadog | semantic→semantic | Fork only downgraded 2 log.info->log.debug (Datadog); upstream rewrote the search fn (15/17), so take upstream and optionally re-apply the downgrades. |
| low | `backend/open_webui/retrieval/web/firecrawl.py` | datadog | semantic→semantic | Fork only downgraded one log.info->log.debug; upstream added a large feature (214/20), so take upstream and optionally re-apply the downgrade. |
| low | `backend/open_webui/retrieval/web/ollama.py` | datadog | semantic→semantic | Fork only downgraded 2 log.info->log.debug (Datadog); upstream rewrote the search fn (10/10), so take upstream and optionally re-apply the downgrades. |
| low | `backend/open_webui/routers/channels.py` | other | semantic→semantic | Fork's only change is re-leveling one notification-post-failure log from info to warning; adopt upstream's async AsyncSession/Config/events rewrite wholesale and optionally re-apply that one log.warning. |
| low | `backend/open_webui/routers/configs.py` | config | semantic→semantic | Fork only raised the OAuth 2.1 discovery-parse-failure log from info to warning in verify_tool_servers_config; take upstream's async Config-namespace rewrite and re-apply that log level if the path survives. |
| low | `backend/open_webui/routers/files.py` | other | semantic→semantic | Fork's only changes are three log-level tweaks (unsupported-type info->warning, content_type/file_path info->debug); adopt upstream's async DB + async-vector-client rewrite and optionally re-apply the log levels. |
| low | `backend/open_webui/routers/images.py` | other | semantic→semantic | Fork's only change is one log.info->log.debug on the ComfyUI workflow class_type in get_models; take upstream's async session_pool/Config/aiohttp rewrite and optionally re-apply the log level. |
| low | `backend/open_webui/routers/knowledge.py` | other | cosmetic→semantic | Fork only lowered 3 log.info->log.debug lines while upstream rewrote the whole knowledge router (~1900 lines); take upstream wholesale, no fork intent to preserve. |
| low | `backend/open_webui/routers/tasks.py` | other | cosmetic→semantic | Fork only lowered one log.info->log.debug in generate_queries; upstream rewrote the router, so take upstream wholesale. |
| low | `backend/open_webui/socket/main.py` | other | cosmetic→semantic | Fork only lowered 4 ydoc join/leave log.info->log.debug lines; upstream heavily rewrote socket handling, so take upstream. |
| low | `backend/start.sh` | deploy | cosmetic→semantic | Fork only added a Datadog doc comment above uvicorn; take upstream's rewritten launcher (no ddtrace/APM is re-introduced, so the fork's tracer-free posture is preserved). |
| low | `src/lib/components/admin/Settings/Models/ModelList.svelte` | config | cosmetic→semantic | Fork only added getContext<i18nStore> typing; take upstream's 51-line logic wholesale (the type annotation is applied automatically by upstream's tsx-typed getContext usage). |
| low | `src/lib/components/chat/ContentRenderer/FloatingButtons.svelte` | chat-render | cosmetic→semantic | Fork change is only a getContext<i18nStore> type annotation; take upstream's 346-line reworked version wholesale. |
| low | `src/lib/components/chat/MessageInput/Commands/Prompts.svelte` | other | cosmetic→semantic | Fork change is only a getContext<i18nStore> type annotation; take upstream's 19-line change wholesale. |
| low | `src/lib/components/chat/Messages/MultiResponseMessages.svelte` | chat-render | cosmetic→semantic | Fork change is only the getContext<i18nStore> type annotation with no behavioral intent, so take upstream's version wholesale. |
| low | `src/lib/components/chat/ModelSelector.svelte` | chat-render | cosmetic→semantic | Fork only annotated getContext<i18nStore>; adopt upstream's version outright. |
| low | `src/lib/components/common/ChatList.svelte` | chat-render | cosmetic→semantic | Fork only annotated getContext<i18nStore>; adopt upstream's 78-line update as-is. |
| low | `src/lib/components/common/Dropdown.svelte` | other | cosmetic→semantic | Fork only added the getContext<i18nStore> annotation with no behavioral intent; take upstream's 190-line rewrite wholesale. |
| low | `src/lib/components/common/EmojiPicker.svelte` | other | cosmetic→semantic | Fork's only change is adding the <i18nStore> generic to getContext('i18n'); upstream rewrote the picker (118/69), so take upstream and re-apply the i18nStore type param only if svelte-check baseline complains. |
| low | `src/lib/components/common/SVGPanZoom.svelte` | other | cosmetic→semantic | Fork only added the <i18nStore> generic to getContext('i18n'); upstream refactored the component (10/22), so take upstream. |
| low | `src/lib/components/workspace/Knowledge.svelte` | other | cosmetic→semantic | Fork's sole change is the <i18nStore> generic on getContext('i18n'); upstream changed logic (76/21), so take upstream. |
| low | `src/lib/components/workspace/Knowledge/KnowledgeBase.svelte` | other | cosmetic→semantic | Fork only added the <i18nStore> type param; upstream near-rewrote the file (977/377), so take upstream wholesale. |
| low | `src/lib/components/workspace/Models/ActionsSelector.svelte` | other | cosmetic→semantic | Fork only added <i18nStore> to getContext('i18n'); upstream changed logic (41/28), so take upstream. |
| low | `src/lib/components/workspace/Models/DefaultFiltersSelector.svelte` | other | cosmetic→semantic | Fork only added the <i18nStore> generic; upstream changed logic (44/22), so take upstream. |
| low | `src/lib/components/workspace/Models/FiltersSelector.svelte` | other | cosmetic→semantic | Fork only added the <i18nStore> generic; upstream changed logic (52/29), so take upstream. |
| low | `src/lib/components/workspace/Models/SkillsSelector.svelte` | other | cosmetic→semantic | Fork's only change is the svelte-check i18n type cast (getContext<i18nStore>); take upstream's 34/25-line rewrite and optionally re-apply the trivial <i18nStore> cast. |
| low | `src/lib/components/workspace/Models/ToolsSelector.svelte` | other | cosmetic→semantic | Fork's only change is the svelte-check i18n type cast (getContext<i18nStore>); take upstream's 36/29-line rewrite and optionally re-apply the trivial <i18nStore> cast. |

## `keep-ours` (15)

| Risk | File | Subsystem | Fork→Upstream | Note |
|---|---|---|---|---|
| high | `src/lib/utils/marked/extension.ts` | chat-render | semantic→semantic | Fork's detailsStart already supersedes upstream's tiny /^<details[\s>]/ fix (matches anywhere, returns index) and adds unclosed-tag streaming support for agent-activity <details> blocks, so keep fork's tokenizer. |
| med | `.env.example` | config | semantic→semantic | Fork fully rewrote this as the Edison env template (DD_ENV/LOG_FORMAT Datadog, AZURE_STORAGE_*/aos-pipeline, VECTOR_DB, WEBUI_SECRET_KEY); keep-ours and optionally append upstream's new doc-only vars (ENABLE_MEMORY_SYSTEM_CONTEXT, VALKEY_*). |
| med | `.github/workflows/deploy-to-hf-spaces.yml` | deploy | branding→deletion | Upstream deleted this workflow but fork owns it and only rebrands the HF Space title to 'Edison AI'; keep-ours (do NOT accept the upstream deletion). |
| med | `docker/Dockerfile` | deploy | semantic→none | Fork-owned new file (v0.10.2 has no docker/Dockerfile, only root Dockerfile) carrying the HF_HUB_DISABLE_XET=1 fix; keep the fork's file, no upstream conflict exists. |
| low | `CHANGELOG.md` | none | deletion→cosmetic | Fork intentionally gutted the changelog to a one-line stub; keep the fork's stub and discard upstream's 1288 added lines. |
| low | `backend/open_webui/retrieval/loaders/external_web.py` | branding | branding→cosmetic | Fork rebranded the External Web Loader User-Agent header to 'Edison AI'; upstream only reordered imports and switched quote style while keeping 'Open WebUI'-keep the Edison string. |
| low | `backend/open_webui/retrieval/web/searchapi.py` | other | semantic→cosmetic | Fork only downgraded the full-JSON result dump from log.info to log.debug (noise reduction); upstream just reflowed double-quotes to single-quotes with no logic change, so keep-ours and optionally re-run the formatter. |
| low | `backend/open_webui/retrieval/web/serpapi.py` | other | semantic→cosmetic | Fork only changed the result-dump log.info to log.debug; upstream change is a pure quote-style reformat with no logic, so keep-ours to preserve the noise reduction. |
| low | `backend/open_webui/retrieval/web/serply.py` | other | semantic→cosmetic | Fork downgraded two log.info calls to log.debug; upstream only adds 'from __future__ import annotations' plus quote/type-hint modernization (no runtime logic), so keep-ours. |
| low | `backend/open_webui/retrieval/web/yacy.py` | branding | branding→cosmetic | Fork set the request User-Agent to 'Edison AI ...'; upstream only reflows quotes and reorders an import, so keep-ours to retain the Edison branding. |
| low | `backend/open_webui/static/favicon.ico` | branding | branding→cosmetic | Binary favicon replaced by fork branding; keep the fork's asset and ignore upstream's icon swap. |
| low | `backend/open_webui/static/favicon.png` | branding | branding→cosmetic | Binary favicon replaced by fork branding; keep the fork's asset and ignore upstream's icon swap. |
| low | `static/favicon.png` | branding | branding→cosmetic | Binary Edison-branded favicon; keep fork's asset over upstream's Open WebUI icon swap. |
| low | `static/static/favicon.ico` | branding | branding→cosmetic | Binary Edison-branded favicon.ico; keep fork's asset over upstream's icon swap. |
| low | `static/static/favicon.png` | branding | branding→cosmetic | Binary Edison-branded favicon; keep fork's asset over upstream's Open WebUI icon swap. |

## `hand-merge` (56)

| Risk | File | Subsystem | Fork→Upstream | Note |
|---|---|---|---|---|
| high | `backend/open_webui/config.py` | config | semantic→semantic | Adopt upstream's ~5.7k-line config refactor but re-add the fork's PersistentConfig blocks: ENABLE_LANGGRAPH_AGENT, STORAGE_PROVIDER/AZURE_PIPELINE_CONTAINER_NAME, OLM_OCR_API_BASE_URL/OLM_OCR_MODEL, and Edison branding. |
| high | `backend/open_webui/env.py` | config | semantic→semantic | Take upstream's ~1.3k-line env refactor but preserve fork's LOG_FORMAT + DD_SERVICE/DD_ENV Datadog tagging, WEBUI_NAME='Edison AI' branding, and LANGGRAPH_AGENT_* tunables. |
| high | `backend/open_webui/main.py` | agent-pipeline | semantic→semantic | Rebase onto upstream's ~3.5k-line async-app refactor while re-applying fork's OLM_OCR import+app.state wiring, extended_thinking param plumbing, self-heal ENABLE_SIGNUP-when-no-users, Windows UnicodeEncodeError banner fallback, and Edison branding/log-level tweaks. |
| high | `backend/open_webui/retrieval/loaders/main.py` | ocr | semantic→semantic | Fork added `from ...olmocr import OlmOCRLoader` plus an `elif self.engine=='olm_ocr'` branch into the loader-dispatch ladder that upstream also rewrote (new paddleocr_vl branch, changed mineru file-ext logic)-re-insert the olm_ocr import and branch into upstream's new ladder or the fork's OCR engine silently disappears. |
| high | `backend/open_webui/routers/auths.py` | sso | semantic→semantic | MUST re-apply the fork's SIGNUP_ALLOWED_EMAIL_DOMAIN='addmangroup.com' gate in signup() and the removal of the auto-disable ENABLE_SIGNUP block, plus the LDAP log.info->log.debug tweaks, onto upstream's async get_async_session/Config/events rewrite. |
| high | `backend/open_webui/routers/openai.py` | agent-pipeline | semantic→semantic | Fork adds claude_thinking_handler() and extended_thinking gating (enable thinking only when toggled AND no tools, since LangGraph handles the think->call->analyse cycle) plus Edison error-string branding; re-apply both onto upstream's rewritten generate_chat_completion. |
| high | `backend/open_webui/routers/pipelines.py` | azure-storage | semantic→semantic | Fork uploads pipeline files to the Azure pipeline container via get_pipeline_storage_provider() and writes a .tmp file to avoid uvicorn --reload crashes, with reworked timeout/error handling; preserve this over upstream's upload_pipeline changes. |
| high | `backend/open_webui/routers/retrieval.py` | ocr | semantic→semantic | Fork threads OLM_OCR_API_BASE_URL/OLM_OCR_MODEL through get_rag_config, ConfigForm, update_rag_config and process_file's Loader call (plus log-level tweaks); re-apply the OlmOCR wiring onto upstream's large retrieval rewrite. |
| high | `backend/open_webui/storage/provider.py` | azure-storage | semantic→semantic | Fork deleted the S3 and GCS providers (Azure-only) and added AZURE_PIPELINE_CONTAINER_NAME support plus get_pipeline_storage_provider(); keep the Azure-only shape and pipeline helper while adopting any upstream Azure fixes, and do NOT let upstream reintroduce S3/GCS (avoids boto3/google-cloud deps). |
| high | `backend/open_webui/utils/logger.py` | datadog | semantic→semantic | Both sides added a JSON stdout sink under LOG_FORMAT==json, but upstream emits plain trace_id/span_id while the fork emits Datadog reserved dd.trace_id/dd.span_id (decimal, lower-64-bit) plus DD_SERVICE/DD_ENV tagging — keep the fork's dd.* correlation and service tags or log-trace linking silently breaks. |
| high | `backend/open_webui/utils/middleware.py` | agent-pipeline | semantic→semantic | Fork adds the whole LangGraph agent mode (function_calling_mode=='langgraph', run_langgraph_agent, langgraph_output streaming), the extended_thinking param, and the internal-tag stripping regex (details/thinking/chart_plan/plan/SYSTEM INSTRUCTION/images) — all must be re-applied onto upstream's heavily rewritten middleware. |
| high | `backend/open_webui/utils/oauth.py` | sso | semantic→semantic | Upstream lacks the fork's SSO work — re-apply _resolve_user_name (Keycloak given_name/family_name/preferred_username fallback), server-side initials-avatar generation instead of bare /user.png, the don't-clobber-existing-avatar guard, and the 'Edison AI' client_name branding onto upstream's restructured OAuthManager. |
| high | `backend/requirements.txt` | deps | semantic→semantic | Take upstream's dep set but keep the fork's OCR/agent deps (pymupdf==1.25.5, langgraph>=0.2.0) and its removal of boto3/google-cloud-storage (fork uses Azure storage), re-pinning sentence-transformers==5.2.2. |
| high | `pyproject.toml` | deps | semantic→semantic | Take upstream's dependency table but re-apply the fork's Edison AI description/branding, pymupdf add, and boto3/google-cloud-storage/moto/gcp-storage-emulator removals (Azure-only storage). |
| high | `src/lib/components/admin/Settings/Documents.svelte` | ocr | semantic→semantic | Both add branches to the same CONTENT_EXTRACTION_ENGINE <select>/if-chain and validate blocks; take upstream (paddleocr_vl, external headers, embedding concurrency, mineru extensions) then re-apply the fork's olm_ocr <option>, OLM_OCR_API_BASE_URL required validation, and OLM_OCR_API_BASE_URL/OLM_OCR_MODEL input branch. |
| high | `src/lib/components/channel/Messages/Message.svelte` | chat-render | semantic→semantic | Take upstream's structural rewrite (swipe-to-reply, id/renderedMessageId props, message.data===true lazy-load) but re-apply the fork's shared_chat card block (cloneSharedChatById forkSharedChat + Open/Fork buttons) which upstream does not have. |
| high | `src/lib/components/chat/Chat.svelte` | agent-pipeline | semantic→semantic | Take upstream's ~2000-line rewrite but re-apply the fork's socket-event handling: statusHistory rebuilt via reassignment + history.messages[id]=message reactivity trigger, and embeds accumulated ([...prev, ...new]) instead of replaced (data.embeds) — required for agent-pipeline streaming/embeds. |
| high | `src/lib/components/chat/Messages/CodeBlock.svelte` | chat-render | semantic→semantic | Both edit the pyodide onmessage handler and render()/output regions; take upstream's shared-pyodide-worker refactor + hljs.highlight + unescapeHtml import, then re-apply the fork's Plotly pipeline (isPlotlyBlock/isMermaidBlock, renderPlotlyVisualization iframe, data:text/html base64 file handling) and note the fork intentionally dropped the data['result'] assignment upstream keeps. |
| high | `src/lib/components/chat/Messages/Markdown/MarkdownTokens.svelte` | chat-render | semantic→semantic | Fork adds a {#key token.attributes?.id} remount around ToolCallDisplay plus a details `open` override tied to agent tool-call/thinking rendering; take upstream's 123-line token rewrite but re-apply the fork's {#key} wrapper and open-attribute logic (i18n typing is throwaway cosmetic). |
| high | `src/lib/components/chat/Messages/ResponseMessage.svelte` | agent-pipeline | semantic→semantic | Fork adds the agent-activity Thinking/Thought-process section (extractAgentActivity, displayContent replacing message.content, expandable UI); take upstream's 171/95 rewrite of the response-content block but re-apply the extractAgentActivity import, the reactive activities/displayContent block, and the collapsible activity renderer or the pipeline's thinking output disappears. |
| high | `src/lib/components/chat/Settings/Advanced/AdvancedParams.svelte` | agent-pipeline | semantic→semantic | Fork adds an `extended_thinking` param toggle and a third `langgraph` function_calling mode (Default/Native/LangGraph cycle + tooltip); take upstream's 114/35 param additions but re-apply the extended_thinking default+UI and the langgraph branch, since the agent pipeline reads these params. |
| high | `src/lib/components/chat/ShareChatModal.svelte` | chat-render | semantic→semantic | Fork removed 'Share to Open WebUI Community' and added a fork-only 'Share to channel' feature (getChannels/sendMessage, shareToChannel posting a shared_chat card, channel <select>); take upstream's 104/69 rewrite but re-apply the channel-share logic and keep community sharing removed. |
| high | `src/routes/+layout.svelte` | sso | semantic→semantic | Re-apply fork's SSO token-cookie->localStorage bridge + /auth redirect, 'Edison AI' notification titles, and English-lock initI18n() onto upstream's heavily restructured (468/99) onMount/setupSocket flow. |
| high | `src/routes/auth/+page.svelte` | sso | semantic→semantic | Keep fork's full Edison-branded SSO-only auth rewrite ('Sign in with SSO', 'Powered by Edison AI · Addman'); selectively adopt upstream's OAUTH_AUTO_REDIRECT block and a11y attrs (aria-hidden/screenReader/alt). |
| med | `backend/open_webui/__init__.py` | deploy | semantic→semantic | Take upstream's Windows psycopg loop='none' fix + WEBUI_SECRET_KEY_LENGTH logic, then re-apply the fork's 'Edison AI version' string and dev-mode reload_excludes=['data/*','uploads/*']. |
| med | `backend/open_webui/models/auths.py` | datadog | semantic→semantic | Upstream converted these methods to async + lazy logging and already dropped the raw api_key from logs; adopt those signatures and re-apply the fork's log.info->log.debug verbosity reduction where still wanted. |
| med | `backend/open_webui/retrieval/vector/dbs/pgvector.py` | branding | branding→semantic | Take upstream's large rewrite (228/217) but re-apply the fork's 'Edison AI' string in the index-rebuild error message (and optional log.info->log.debug downgrades). |
| med | `backend/open_webui/retrieval/vector/type.py` | other | deletion→semantic | Adopt upstream's new MARIADB_VECTOR and VALKEY enum entries (and quote style) but re-remove the S3VECTOR line to stay consistent with the deleted s3vector.py. |
| med | `backend/open_webui/retrieval/web/external.py` | branding | branding→semantic | Upstream still ships the 'Open WebUI' User-Agent; take upstream's rewrite but re-apply the fork's 'Edison AI' User-Agent (and optional log.info->log.debug). |
| med | `backend/open_webui/routers/audio.py` | other | semantic→semantic | Re-apply the fork's Redis speech/transcription caching (redis_cache_get/set + _sync_redis), the load_dataset revision='refs/convert/parquet' fix, the whisper auto_update/None-guard, 'Edison AI' error branding, and log-level tweaks onto upstream's async Config/session_pool rewrite. |
| med | `backend/open_webui/routers/users.py` | user-avatar | semantic→semantic | Fork adds generate_initials_image_data_url import and an on-the-fly initials-avatar fallback in get_user_profile_image_by_id for users without a stored image; re-apply this block onto upstream's rewritten users router. |
| med | `backend/open_webui/utils/auth.py` | other | semantic→semantic | Fork wraps Redis get/set in try/except to gracefully degrade when Redis is unreachable; re-apply that resilience onto upstream's refactored is_valid_token(decoded, redis) signature and new per-user revoked_at OIDC-logout path. |
| med | `backend/open_webui/utils/misc.py` | user-avatar | semantic→semantic | Fork adds get_initials() and generate_initials_image_data_url() (server-side SSO initials avatar, imported by oauth.py) plus two log.info->log.warning in stream_chunks_handler; preserve both helpers or oauth.py's import breaks. |
| med | `backend/open_webui/utils/payload.py` | agent-pipeline | semantic→semantic | Fork adds `"extended_thinking": bool` to remove_open_webui_params' type dict (upstream lacks it); re-add that key to upstream's dict or the extended_thinking param leaks through to the model API. |
| med | `backend/open_webui/utils/plugin.py` | other | semantic→semantic | Fork makes install_frontmatter_requirements non-fatal (log.warning + swallow instead of log.error + raise) plus log-level downgrades; adopt upstream's _installed_requirements caching but re-apply the fork's swallow-on-pip-failure behavior. |
| med | `backend/open_webui/utils/redis.py` | other | semantic→semantic | Both refactor get_redis_connection: adopt upstream's socket-options factory but re-apply the fork's fast-fail defaults (retry_on_timeout=False + hard 5s socket_connect_timeout/socket_timeout fallback even when REDIS_SOCKET_CONNECT_TIMEOUT is unset). |
| med | `src/lib/components/admin/Users/UserList.svelte` | branding | branding→semantic | Take upstream's real additions (UserPreviewModal, debounced handleSearchInput, aria-labels, favicon fallback) but re-apply the fork's de-branding that strips the OpenWebUI enterprise-licensing/sponsorship NOTE block (upstream also edits that same note's sponsor URL). |
| med | `src/lib/components/channel/Channel.svelte` | branding | branding→semantic | Take upstream's pinHandler refactor (pinned_by/pinned_at, onPin propagated to Thread, threadId reset on delete) but re-apply the fork's 'Edison AI' <title> branding; the fork's TS null-guards/type annotations are cosmetic svelte-check noise that upstream's version supersedes. |
| med | `src/lib/components/chat/Messages/Markdown/HTMLToken.svelte` | chat-render | semantic→cosmetic | Upstream only cosmetically refactors the reactive block into two $: statements; keep the fork's semantic guard that nulls out inline Plotly.newPlot/Chart()/script chart-blocks before DOMPurify.sanitize (part of the fork's Plotly-via-tool-embed rendering, else charts re-inline in chat). |
| med | `src/lib/components/chat/Settings/General.svelte` | config | semantic→semantic | Fork deliberately removed the Language selector block, getLanguages/changeLanguage wiring, and the 'Help us translate Open WebUI' link; adopt upstream's General.svelte changes but keep that whole language/translate section deleted. |
| med | `src/lib/components/chat/ToolServersModal.svelte` | branding | branding→semantic | Fork's only real change is rebranding 'Open WebUI can use tools...' to 'Edison AI...'; take upstream's 53/15 logic additions and re-apply the Edison AI string. |
| med | `src/lib/components/common/FullHeightIframe.svelte` | chat-render | semantic→semantic | Fork rewrote the iframe template into a wrapper div with corner drag-to-resize + MutationObserver auto-sizing (for Plotly/chart embeds); upstream added injectCsp on the same srcdoc line plus config/injectCsp imports — keep fork's drag/observer container but adopt upstream's srcdoc CSP injection. |
| med | `src/lib/components/common/RichTextInput.svelte` | chat-render | semantic→semantic | Fork restructured the Editor extensions array to disable StarterKit codeBlock/lists (TipTap 3.20 duplicate keyed-plugin fix) and split re-render vs onUpdate handlers; upstream heavily rewrote editor init (167/46) — take upstream base and re-apply the fork's StarterKit disables and onUpdate htmlValue handler. |
| med | `src/lib/components/common/ToolCallDisplay.svelte` | chat-render | semantic→semantic | Fork deliberately removed the embeds/FullHeightIframe rendering branch (fork renders charts via its own pipeline) while upstream expanded that same branch (grouped check + iframeSandbox settings, 62/20) — take upstream's tool-call display improvements but re-drop the embeds branch and its FullHeightIframe import/attribute per fork intent. |
| med | `src/lib/components/layout/Sidebar/ChatItem.svelte` | chat-render | semantic→semantic | Fork wraps showShareChatModal and showDeleteConfirm in setTimeout(100ms) to stop an infinite focus-trap loop between dropdown and confirm dialog; upstream massively refactored ChatItem (308/182) — take upstream base and re-apply the two setTimeout-guarded modal openers. |
| med | `src/lib/components/layout/Sidebar/UserMenu.svelte` | branding | branding→semantic | Fork commented out the admin Documentation and Releases DropdownMenu.Item entries (removes OpenWebUI external links) plus i18n typing; upstream rewrote UserMenu (372/88) — take upstream and re-remove/comment those two external-link menu items. |
| med | `src/lib/i18n/index.ts` | other | semantic→semantic | Keep fork's English-only lock (removed LanguageDetector, hardcoded lng/fallbackLng='en-US', initI18n() no-arg) while adopting upstream's new languageChanged handler that sets document lang attribute. |
| low | `backend/open_webui/retrieval/web/mojeek.py` | datadog | semantic→semantic | Upstream still has a stray print(results) at line 28; take upstream's rewrite but re-apply the fork's print->log.debug so raw output doesn't pollute Datadog structured stdout. |
| low | `backend/open_webui/retrieval/web/searxng.py` | branding | branding→semantic | Adopt upstream's async rewrite (search_searxng is now async via utils.session_pool.get_session with a module-level _SEARXNG_HEADERS dict), then set _SEARXNG_HEADERS['User-Agent'] back to the fork's 'Edison AI ...' string. |
| low | `backend/open_webui/retrieval/web/yandex.py` | branding | branding→semantic | Keep upstream's new 'if element is None: return ' guard in xml_element_contents_to_string, then re-apply the fork's 'Edison AI' User-Agent and the results-dump log.info->log.debug. |
| low | `backend/open_webui/routers/ollama.py` | branding | branding→semantic | Fork rebrands HTTPException detail strings 'Open WebUI'->'Edison AI' (plus a few log.info->debug); take upstream's rewritten router and re-apply the 'Edison AI: Server Connection Error' strings. |
| low | `backend/open_webui/routers/scim.py` | sso | branding→semantic | Fork only rebrands the module docstring to 'Edison AI' and lowers one log level; take upstream's rewritten SCIM logic and re-apply the Edison docstring string. |
| low | `backend/open_webui/utils/chat.py` | datadog | semantic→semantic | Fork only downgrades 4 log.info->log.debug (and fixes a stray logging.info->log) in generate_direct_chat_completion for Datadog noise reduction; take upstream logic and re-apply the debug downgrades. |
| low | `backend/open_webui/utils/files.py` | datadog | semantic→semantic | Upstream still uses print() in load_b64_audio_data with no module logger; take upstream logic and re-apply the fork's module `log` + print->log.exception logging cleanup. |
| low | `backend/open_webui/utils/images/comfyui.py` | datadog | semantic→semantic | Fork only downgrades several log.info->log.debug in queue/get/websocket helpers for Datadog noise reduction; take upstream logic and re-apply the debug downgrades. |
| low | `backend/open_webui/utils/models.py` | datadog | semantic→semantic | Fork only retunes log severities (info->warning for load failures, info->debug for not-found) in get_all_models; take upstream logic and re-apply the level tweaks. |

## `keep-deleted` (12)

| Risk | File | Subsystem | Fork→Upstream | Note |
|---|---|---|---|---|
| low | `.github/FUNDING.yml` | other | deletion→cosmetic | Fork deleted the upstream sponsorship file; upstream only tweaked the sponsor handle, so keep-deleted. |
| low | `.github/ISSUE_TEMPLATE/config.yml` | other | deletion→semantic | Fork removed upstream's issue-template config; upstream added contact links, but fork does not use these templates, so keep-deleted. |
| low | `.github/ISSUE_TEMPLATE/feature_request.yaml` | other | deletion→semantic | Fork deleted the upstream feature-request template; upstream reworked it, but keep-deleted as it is upstream project-meta the fork doesn't use. |
| low | `.github/dependabot.yml` | other | deletion→semantic | Fork deleted this project-meta file; upstream still ships/edits it, so keep it deleted (fork does not run upstream dependabot). |
| low | `.github/pull_request_template.md` | other | deletion→semantic | Fork deleted upstream's PR template; upstream edited it, but keep-deleted since the fork runs its own PR process. |
| low | `CONTRIBUTOR_LICENSE_AGREEMENT` | none | deletion→cosmetic | Fork deleted this project-meta file; keep it deleted and ignore upstream's minor text change. |
| low | `LICENSE` | none | deletion→cosmetic | Fork removed the upstream LICENSE; keep it deleted rather than re-adopting upstream's revised text. |
| low | `README.md` | branding | deletion→cosmetic | Fork deleted the upstream README (removing Open WebUI marketing/branding); keep it deleted and ignore upstream's updates. |
| low | `backend/open_webui/retrieval/vector/dbs/s3vector.py` | other | deletion→semantic | Fork intentionally dropped S3Vector support (778-line delete); keep it deleted and also drop S3VECTOR from type.py plus any connector-factory reference. |
| low | `contribution_stats.py` | none | deletion→semantic | Fork removed this upstream project-meta script; keep it deleted despite upstream's edits. |
| low | `docker-compose.playwright.yaml` | none | deletion→cosmetic | Fork removed this upstream test-infra compose file; keep it deleted. |
| low | `docs/SECURITY.md` | none | deletion→cosmetic | Fork deleted upstream security policy doc; keep it deleted and drop upstream's edits. |

## `accept-upstream-deletion` (5)

| Risk | File | Subsystem | Fork→Upstream | Note |
|---|---|---|---|---|
| med | `src/lib/components/admin/Settings/Models/ConfigureModelsModal.svelte` | config | cosmetic→deletion | Fork change is only an i18nStore type annotation; upstream deletes the file, so accept the deletion but also drop the import in the fork's admin/Settings/Models.svelte (which still imports it). |
| med | `src/lib/components/admin/Settings/Tools.svelte` | config | cosmetic→deletion | Fork change is only an i18nStore type annotation; upstream deletes this admin Tools panel, so accept deletion and remove the './Settings/Tools.svelte' imports still present in the fork's admin/Settings.svelte and chat/SettingsModal.svelte. |
| med | `src/lib/components/chat/Settings/Personalization/AddMemoryModal.svelte` | other | cosmetic→deletion | Fork only added an i18n type annotation; upstream deleted this file and its own ManageModal no longer imports it, so accept the deletion and ensure ManageModal.svelte is taken from upstream so nothing still imports AddMemoryModal. |
| med | `src/lib/components/chat/Settings/Personalization/EditMemoryModal.svelte` | other | cosmetic→deletion | Fork only added an i18n type annotation; upstream deleted this file and refactored ManageModal to stop importing it, so accept the deletion and adopt upstream's ManageModal.svelte to avoid a dangling EditMemoryModal import. |
| low | `backend/open_webui/test/apps/webui/storage/test_provider.py` | azure-storage | deletion→deletion | Fork trimmed the test to Azure-only (removed boto3/moto/gcp imports and cases); upstream deleted the whole file, so drop it rather than porting the Azure-only trims. |
