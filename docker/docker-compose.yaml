services:
  aos-gpt:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: addmangroup/aos-gpt:latest
    container_name: aos-gpt
    volumes:
      - aos-gpt:/app/backend/data
    ports:
      - ${OPEN_WEBUI_PORT-1776}:1776
    env_file:
      - ../.env
    environment:
      # Defaults (only used if not set in .env)
      - CORS_ALLOW_ORIGIN=${CORS_ALLOW_ORIGIN-*}
      - FORWARDED_ALLOW_IPS=${FORWARDED_ALLOW_IPS-*}
      - SCARF_NO_ANALYTICS=${SCARF_NO_ANALYTICS-true}
      - DO_NOT_TRACK=${DO_NOT_TRACK-true}
      - ANONYMIZED_TELEMETRY=${ANONYMIZED_TELEMETRY-false}
    extra_hosts:
      - host.docker.internal:host-gateway
    restart: unless-stopped

  pipelines:
    build:
      context: ..
      dockerfile: docker/Dockerfile.pipelines
    image: addmangroup/aos-pipelines:latest
    volumes:
      - pipelines:/app/pipelines
    restart: unless-stopped
    ports:
      - ${PIPELINES_PORT-9099}:9099
    env_file:
      - ../.env
    environment:
      - PIPELINES_API_KEY=0p3n-w3bu!
      # Override DD_SERVICE so the pipelines container shows up as a distinct
      # Datadog service from aos-gpt — DD_ENV / DD_VERSION / LOG_FORMAT are
      # inherited from the shared .env file.
      - DD_SERVICE=aos-pipelines

volumes:
  aos-gpt: {}
  pipelines: {}


# Usage:
#   Build locally:  docker compose -f docker/docker-compose.yaml build
#   Push to hub:    docker compose -f docker/docker-compose.yaml push
#   Build & push:   docker compose -f docker/docker-compose.yaml build && docker compose -f docker/docker-compose.yaml push
#   Run:            docker compose -f docker/docker-compose.yaml up -d
